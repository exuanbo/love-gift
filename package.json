{
  "name": "love-gift",
  "version": "0.0.1",
  "description": "My love gift to my future girlfriend.",
  "scripts": {
    "clean": "rimraf dist && mkdir dist",
    "compile": "coffee -c -o dist/ src/index.coffee",
    "compile:watch": "coffee -w -c -o dist/ src/index.coffee",
    "misc": "cp src/favicon.ico src/index.html dist/",
    "build": "yarn clean && yarn misc && yarn compile",
    "dev": "yarn clean && yarn misc && yarn compile:watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/exuanbo/love-gift.git"
  },
  "author": "exuanbo",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/exuanbo/love-gift/issues"
  },
  "homepage": "https://github.com/exuanbo/love-gift#readme",
  "devDependencies": {
    "coffeescript": "2.5.1",
    "rimraf": "3.0.2"
  }
}
